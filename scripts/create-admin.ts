// 이 스크립트는 API 라우트를 통해 실행됩니다.
// 서버가 실행 중일 때 다음 명령으로 호출하세요:
// curl -X POST http://localhost:3000/api/admin/create-user -H "Content-Type: application/json" -d "{\"email\":\"admin@modoolecture.com\",\"password\":\"modoo@82\"}"

// 또는 브라우저에서 개발자 도구 콘솔에서:
// fetch('/api/admin/create-user', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ email: 'admin@modoolecture.com', password: 'modoo@82' }) }).then(r => r.json()).then(console.log)

console.log('이 스크립트는 더 이상 사용되지 않습니다.');
console.log('대신 API 라우트를 사용하세요: POST /api/admin/create-user');

