# 배포 환경 목록 미표시 문제 상세 보고서

## 📋 문제 개요

**증상**: 로컬 환경에서는 질문 목록이 정상적으로 표시되지만, Vercel 배포 환경에서는 목록이 표시되지 않음

**영향 범위**: 
- `/admin/events/[eventId]/questions` 페이지
- 질문 목록 조회 기능

**발생 시점**: 배포 후 (로컬에서는 정상 작동)

---

## 🔍 원인 분석

### 1. 환경 변수 문제 (가능성: 높음)

#### 1.1 문제점
- Vercel 환경 변수 설정 누락 또는 불일치
- `SUPABASE_SERVICE_ROLE_KEY`가 배포 환경에 설정되지 않았을 가능성
- 환경 변수 이름 오타 또는 값 불일치

#### 1.2 확인 사항
```typescript
// lib/supabase/admin.ts
const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL!;
const serviceRoleKey = process.env.SUPABASE_SERVICE_ROLE_KEY!;
```

**필수 환경 변수**:
- `NEXT_PUBLIC_SUPABASE_URL` (공개 변수)
- `SUPABASE_SERVICE_ROLE_KEY` (서버 전용, 비공개)

#### 1.3 진단 방법
1. Vercel 대시보드 → Settings → Environment Variables 확인
2. API 라우트에 디버깅 로그 추가하여 환경 변수 존재 여부 확인
3. Vercel 함수 로그에서 환경 변수 관련 오류 확인

---

### 2. API 라우트 오류 처리 문제 (가능성: 중간)

#### 2.1 현재 코드 분석
```typescript
// app/api/admin/questions/route.ts
export async function GET(request: NextRequest) {
  try {
    const { searchParams } = new URL(request.url);
    const eventId = searchParams.get('event_id');

    if (!eventId) {
      return NextResponse.json(
        { error: 'event_id는 필수입니다.' },
        { status: 400 }
      );
    }

    const { data, error } = await supabaseAdmin
      .from('modu_questions')
      .select('*')
      .eq('event_id', eventId)
      .order('created_at', { ascending: false });

    if (error) {
      console.error('질문 목록 조회 실패:', error);
      return NextResponse.json(
        { error: '질문 목록을 불러올 수 없습니다.' },
        { status: 500 }
      );
    }

    return NextResponse.json({ questions: data || [] });
  } catch (error) {
    console.error('예상치 못한 오류:', error);
    return NextResponse.json(
      { error: '서버 오류가 발생했습니다.' },
      { status: 500 }
    );
  }
}
```

#### 2.2 문제점
- 에러 메시지가 클라이언트에 전달되지만, 상세한 디버깅 정보 부족
- `supabaseAdmin` 초기화 실패 시 명확한 오류 메시지 없음
- 환경 변수 누락 시 런타임 에러 발생 가능

---

### 3. Supabase Admin 클라이언트 초기화 문제 (가능성: 높음)

#### 3.1 현재 구현
```typescript
// lib/supabase/admin.ts
import { createClient } from '@supabase/supabase-js';

const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL!;
const serviceRoleKey = process.env.SUPABASE_SERVICE_ROLE_KEY!;

if (!supabaseUrl || !serviceRoleKey) {
  throw new Error('Supabase Admin 환경 변수가 설정되지 않았습니다.');
}

export const supabaseAdmin = createClient(supabaseUrl, serviceRoleKey, {
  auth: {
    autoRefreshToken: false,
    persistSession: false,
  },
});
```

#### 3.2 문제점
- 모듈 로드 시점에 환경 변수가 없으면 즉시 에러 발생
- 빌드 타임에는 환경 변수가 없어도 통과할 수 있음
- 런타임에 환경 변수가 없으면 모든 API 호출 실패

---

### 4. 클라이언트 사이드 에러 처리 부족 (가능성: 중간)

#### 4.1 현재 구현
```typescript
// app/admin/events/[eventId]/questions/page.tsx
const loadQuestions = async () => {
  try {
    setLoading(true);
    const response = await fetch(`/api/admin/questions?event_id=${eventId}`);
    const data = await response.json();

    if (!response.ok) {
      throw new Error(data.error || '질문 목록을 불러올 수 없습니다.');
    }

    setQuestions(data.questions || []);
    setError(null);
  } catch (err) {
    setError(err instanceof Error ? err.message : '오류가 발생했습니다.');
  } finally {
    setLoading(false);
  }
};
```

#### 4.2 문제점
- 네트워크 오류 시 상세 정보 부족
- API 응답 상태 코드별 처리 부족
- 사용자에게 명확한 오류 메시지 표시 부족

---

### 5. 데이터베이스 RLS 정책 문제 (가능성: 낮음)

#### 5.1 설명
- `supabaseAdmin`은 Service Role Key를 사용하므로 RLS를 우회해야 함
- 하지만 RLS 정책이 잘못 설정되어 있으면 데이터 조회 실패 가능

#### 5.2 확인 사항
- Supabase 대시보드에서 RLS 정책 확인
- `modu_questions` 테이블의 SELECT 정책 확인

---

## 🛠️ 해결 방안

### 방안 1: 환경 변수 확인 및 설정 (우선순위: 최고)

#### 1.1 Vercel 환경 변수 확인
1. Vercel 대시보드 접속
2. 프로젝트 → Settings → Environment Variables
3. 다음 변수들이 모든 환경(Production, Preview, Development)에 설정되어 있는지 확인:
   ```
   NEXT_PUBLIC_SUPABASE_URL=your-supabase-url
   NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
   SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
   ```

#### 1.2 환경 변수 검증 API 추가
```typescript
// app/api/admin/debug/env/route.ts (임시 디버깅용)
import { NextResponse } from 'next/server';

export async function GET() {
  return NextResponse.json({
    hasSupabaseUrl: !!process.env.NEXT_PUBLIC_SUPABASE_URL,
    hasAnonKey: !!process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY,
    hasServiceKey: !!process.env.SUPABASE_SERVICE_ROLE_KEY,
    urlLength: process.env.NEXT_PUBLIC_SUPABASE_URL?.length || 0,
    keyLength: process.env.SUPABASE_SERVICE_ROLE_KEY?.length || 0,
  });
}
```

---

### 방안 2: API 라우트 에러 처리 개선 (우선순위: 높음)

#### 2.1 상세한 에러 로깅 추가
```typescript
// app/api/admin/questions/route.ts 수정
export async function GET(request: NextRequest) {
  try {
    // 환경 변수 확인
    if (!process.env.NEXT_PUBLIC_SUPABASE_URL || !process.env.SUPABASE_SERVICE_ROLE_KEY) {
      console.error('환경 변수 누락:', {
        hasUrl: !!process.env.NEXT_PUBLIC_SUPABASE_URL,
        hasKey: !!process.env.SUPABASE_SERVICE_ROLE_KEY,
      });
      return NextResponse.json(
        { 
          error: '서버 설정 오류: 환경 변수가 설정되지 않았습니다.',
          details: '관리자에게 문의하세요.'
        },
        { status: 500 }
      );
    }

    const { searchParams } = new URL(request.url);
    const eventId = searchParams.get('event_id');

    if (!eventId) {
      return NextResponse.json(
        { error: 'event_id는 필수입니다.' },
        { status: 400 }
      );
    }

    const { data, error } = await supabaseAdmin
      .from('modu_questions')
      .select('*')
      .eq('event_id', eventId)
      .order('created_at', { ascending: false });

    if (error) {
      console.error('질문 목록 조회 실패:', {
        error,
        eventId,
        errorCode: error.code,
        errorMessage: error.message,
        errorDetails: error.details,
      });
      return NextResponse.json(
        { 
          error: '질문 목록을 불러올 수 없습니다.',
          details: error.message,
          code: error.code,
        },
        { status: 500 }
      );
    }

    console.log(`질문 목록 조회 성공: ${data?.length || 0}개`);
    return NextResponse.json({ questions: data || [] });
  } catch (error) {
    console.error('예상치 못한 오류:', {
      error,
      message: error instanceof Error ? error.message : 'Unknown error',
      stack: error instanceof Error ? error.stack : undefined,
    });
    return NextResponse.json(
      { 
        error: '서버 오류가 발생했습니다.',
        details: error instanceof Error ? error.message : 'Unknown error',
      },
      { status: 500 }
    );
  }
}
```

---

### 방안 3: 클라이언트 사이드 에러 처리 개선 (우선순위: 중간)

#### 3.1 상세한 에러 표시
```typescript
// app/admin/events/[eventId]/questions/page.tsx 수정
const loadQuestions = async () => {
  try {
    setLoading(true);
    setError(null);
    
    const response = await fetch(`/api/admin/questions?event_id=${eventId}`);
    
    if (!response.ok) {
      const errorData = await response.json();
      console.error('API 오류:', {
        status: response.status,
        statusText: response.statusText,
        error: errorData,
      });
      
      throw new Error(
        errorData.details 
          ? `${errorData.error}: ${errorData.details}`
          : errorData.error || '질문 목록을 불러올 수 없습니다.'
      );
    }

    const data = await response.json();
    setQuestions(data.questions || []);
    setError(null);
  } catch (err) {
    const errorMessage = err instanceof Error 
      ? err.message 
      : '오류가 발생했습니다.';
    console.error('질문 목록 로드 실패:', err);
    setError(errorMessage);
  } finally {
    setLoading(false);
  }
};
```

---

### 방안 4: Supabase Admin 클라이언트 초기화 개선 (우선순위: 높음)

#### 4.1 지연 초기화 패턴 적용
```typescript
// lib/supabase/admin.ts 수정
import { createClient } from '@supabase/supabase-js';

let supabaseAdminInstance: ReturnType<typeof createClient> | null = null;

function getSupabaseAdmin() {
  if (supabaseAdminInstance) {
    return supabaseAdminInstance;
  }

  const supabaseUrl = process.env.NEXT_PUBLIC_SUPABASE_URL;
  const serviceRoleKey = process.env.SUPABASE_SERVICE_ROLE_KEY;

  if (!supabaseUrl || !serviceRoleKey) {
    throw new Error(
      'Supabase Admin 환경 변수가 설정되지 않았습니다.\n' +
      `NEXT_PUBLIC_SUPABASE_URL: ${supabaseUrl ? 'Set' : 'Missing'}\n` +
      `SUPABASE_SERVICE_ROLE_KEY: ${serviceRoleKey ? 'Set' : 'Missing'}`
    );
  }

  supabaseAdminInstance = createClient(supabaseUrl, serviceRoleKey, {
    auth: {
      autoRefreshToken: false,
      persistSession: false,
    },
  });

  return supabaseAdminInstance;
}

export const supabaseAdmin = new Proxy({} as ReturnType<typeof createClient>, {
  get(target, prop) {
    const instance = getSupabaseAdmin();
    const value = instance[prop as keyof typeof instance];
    return typeof value === 'function' ? value.bind(instance) : value;
  },
});
```

---

## 🔬 진단 체크리스트

### 즉시 확인 사항
- [ ] Vercel 환경 변수 설정 확인
  - `NEXT_PUBLIC_SUPABASE_URL` 존재 여부
  - `SUPABASE_SERVICE_ROLE_KEY` 존재 여부
  - 환경 변수 값이 올바른지 확인 (공백, 따옴표 등)
- [ ] Vercel 함수 로그 확인
  - `/api/admin/questions` 엔드포인트 호출 시 에러 확인
  - 환경 변수 관련 에러 메시지 확인
- [ ] 브라우저 개발자 도구 확인
  - Network 탭에서 API 응답 확인
  - Console 탭에서 에러 메시지 확인

### 추가 확인 사항
- [ ] Supabase 프로젝트 상태 확인
  - 프로젝트가 활성화되어 있는지
  - 데이터베이스 연결 상태 확인
- [ ] 네트워크 연결 확인
  - Vercel에서 Supabase로의 네트워크 연결 가능 여부
  - 방화벽 또는 IP 제한 설정 확인

---

## 📊 예상 원인 우선순위

1. **환경 변수 미설정** (80% 확률)
   - Vercel에 `SUPABASE_SERVICE_ROLE_KEY`가 설정되지 않음
   - 환경 변수 이름 오타
   - 환경 변수 값이 잘못됨

2. **Supabase Admin 클라이언트 초기화 실패** (15% 확률)
   - 모듈 로드 시점에 환경 변수 없음
   - 빌드 타임과 런타임 차이

3. **API 라우트 에러 처리 부족** (5% 확률)
   - 에러가 발생했지만 클라이언트에 전달되지 않음
   - 네트워크 오류 처리 부족

---

## 🚀 즉시 조치 사항

### 1단계: 환경 변수 확인
```bash
# Vercel CLI로 환경 변수 확인 (선택사항)
vercel env ls
```

### 2단계: 임시 디버깅 API 추가
위의 "방안 1.2" 참조하여 환경 변수 검증 API 추가

### 3단계: Vercel 로그 확인
1. Vercel 대시보드 → 프로젝트 → Functions
2. 최근 배포의 로그 확인
3. 에러 메시지 확인

### 4단계: 코드 개선 적용
- 방안 2: API 라우트 에러 처리 개선
- 방안 3: 클라이언트 사이드 에러 처리 개선
- 방안 4: Supabase Admin 클라이언트 초기화 개선

---

## 📝 참고 사항

### Next.js 환경 변수 규칙
- `NEXT_PUBLIC_` 접두사: 클라이언트와 서버 모두에서 접근 가능
- 접두사 없음: 서버 사이드에서만 접근 가능
- 빌드 타임 vs 런타임: 서버 컴포넌트는 빌드 타임에 평가될 수 있음

### Vercel 환경 변수 설정
- Production, Preview, Development 환경별로 별도 설정 가능
- 환경 변수 변경 후 재배포 필요
- 환경 변수 값에 공백이나 특수문자가 있으면 따옴표로 감싸야 함

---

## 📅 보고서 작성일
2025-01-16

## 작성자
AI Assistant

